1、ptp_monitor 监听网卡切换，重启 mpuaps 触发重新同步 VO
2、ptp_monitor 监听服务异常，自动重启服务  -- ptp 服务配置异常重启
3、重启主控，使能 VO 异常
4、设备断电重启，设置解码延迟报错  
    -- 主控抓包分析是否通讯异，设备网口 link 状态问题？
    -- RPC连接异常添加重试逻辑
5、多主控环境，配置 ptp 主时钟优先级，防止主时钟频繁选举切换
    -- 多主控环境，定义为主备主时钟环境，防止一个主控板异常，ptp 主时钟
    -- 通过配置主时钟优先级，多主控同时运行时，明确主时钟设备

6、GUI 报错无效的空指针 BUG
7、