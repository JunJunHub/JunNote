# MacOS
/.DS_Store

# JetBrains IDEA
/.idea/

# VSCode
/.settings/
/.vscode/
/.vscode


# 暂不同步的目录
00-PersonalInfo
01-work
02-CodeComponents
03-Tools


# .gitignore 更新
# .gitignore 只能忽略那些原来没有被 track 的文件
# 如果某些文件已经被纳入了版本管理中，则修改 .gitignore 是无效的
# 解决方法就是先把本地缓存删除（改变成未track状态），然后再提交，命令如下：
# git rm -r --cached .
# git add .
# git commit -m "update .gitignore"
# git push
